{
  "name": "timber-v3",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Timber template with Skybolt v3",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/concat-js.js && rm -rf public/assets && cp -r dist/assets public/ && cp dist/skybolt-sw.js public/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@skybolt/vite-plugin": "file:../../packages/vite-plugin",
    "lightningcss": "^1.23.0",
    "vite": "^5.0.0"
  }
}
