[project]
name = "skybolt-django-example"
version = "1.0.0"
description = "Django Skybolt example"
requires-python = ">=3.10"
dependencies = [
    "django>=5.0",
    "skybolt",
]

[tool.uv.sources]
skybolt = { path = "../../packages/python", editable = true }
